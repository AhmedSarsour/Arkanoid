/**
 * Task interface.
 * @author ahmed.
 * @param <T> the type.
 * @since 14.06.2017.
 */
public interface Task<T> {
    /**
     * run the animation.
     * @return the type.
     */
    T run();
}